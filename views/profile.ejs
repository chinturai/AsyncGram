<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile - <%= user.name %> </title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="bg-zinc-700 w-full min-h-screen text-white p-10">

        <h1 class="text-5xl font-bold text-center"> Hello, <%= user.name %> !!  👋👋 </h1>
        <!-- <img src="https://user-images.githubusercontent.com/74038190/216122041-518ac897-8d92-4c6b-9b3f-ca01dcaf38ee.png" alt="Fire" width="120" /> -->
        <!-- <img src="https://user-images.githubusercontent.com/74038190/216122065-2f028bae-25d6-4a3c-bc9f-175394ed5011.png" alt="E-Mail" width="120" /> -->
         
        <div class="bg-zinc-800 rounded-md p-10 max-w-md mx-auto mt-10">
                <div class="max-w-md mx-auto"> 
                    <h1 class="text-3xl font-semibold text-blue-500 ">Your Profile</h1>
                    <h2 class=" text-zinc-400 mt-10">I am <%= user.name %> </h2>
                    <h2 class=" text-zinc-400">Reach me at @<%= user.username %> </h2>
                    <h2 class=" text-zinc-400">Age : <%= user.age %></h2>
                </div>
        </div>

        <div class="bg-zinc-800 rounded-md p-10 max-w-md mx-auto mt-10">
            <div class="max-w-md mx-auto"> 
                <h1 class="text-3xl font-semibold text-blue-500 ">You can create a new post 💬 </h1>
                <form action="/post" method="post" class="mt-5">
                    <textarea class="p-3 outline-none w-full resize-none bg-transparent border-2 border-zinc-600 rounded-md" name="content" id="" placeholder="What's on your mind ? ✍"></textarea>
                    <input type="submit" class="px-3 py-2 bg-green-500 rounded-md mt-4" value="Post ✅">
                </form>
            </div>
        </div>

        <div class="bg-zinc-800 rounded-md p-10 max-w-md mx-auto mt-10">
            <div class="max-w-md mx-auto"> 
                <h1 class="text-3xl font-semibold text-blue-500 ">Your Posts 📌</h1>
                <div class="postcontainer">

                    <% user.posts.reverse().forEach(function(post){ %>
                            <!-- Post Starts-->
                            <div class="bg-zinc-700 rounded-md p-10 max-w-md mx-auto mt-10">
                                <div class="max-w-md mx-auto"> 
                                    <h1 class="text-3xl font-semibold text-green-500 mb-2"><%= user.name %></h1>
                                    <h3 class="text-xl font-semibold text-zinc-500 mb-7">@<%= user.username %></h3>
                                    <p class="tracking-tighter text-zinc-100"><%= post.content %></p>
                                </div>

                                <div class="Numbers">
                                    <small class="text-zinc-400 mt-10 ml-2 inline-block"> <%= post.likes.length %> Likes </small>
                                </div>
                                <div class="btns flex">
                                    <a href="/like/<%= post._id %>" class="bg-zinc-900 rounded-md px-3 py-2 mt-6">
                                        <%= post.likes.indexOf(user._id)===-1 ? "Like👍" : "Unlike👎" %>
                                    </a>

                                    <a href="/edit/<%= post._id %>" class="bg-zinc-900 rounded-md px-3 py-2 mt-6 ml-5">
                                        Edit🖋️
                                    </a>
                                    <a href="/delete/<%= post._id %>" class="bg-zinc-900  rounded-md px-3 py-2 mt-6 ml-5">
                                        Delete🗑️
                                    </a>
                                </div>
                            </div>
                            <!-- Post Ends -->
                    <% }) %>

                </div>
            </div>
        </div>

        <div class="bg-zinc-800 rounded-md p-10 max-w-md mx-auto mt-10">
            <div class="max-w-md mx-auto"> 
                <h1 class="text-3xl font-semibold text-blue-500 mb-10">Account Options 👤</h1>
                <a href="/logout" class="w-[80px] bg-red-500 rounded-md px-3 py-2 block "> Logout </a>
                <a href="/" class="w-1/2 bg-yellow-500 rounded-md px-3 py-2 block mt-5"> Create a new account </a>
            </div>
        </div>

    </div>
</body>
</html>










